# Copy to supabase/functions/.env for local function development.
# Do not commit the copied .env file.

OPENAI_API_KEY=sk-your-openai-key
OPENAI_MODEL=gpt-4o-mini
# Must match the bucket created by migration 20260516090000_receipt_storage.sql
SUPABASE_STORAGE_RECEIPT_BUCKET=receipts
